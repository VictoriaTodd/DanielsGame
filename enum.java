
//controls the state of the game
//this is in a separate file because Processing doesn't support enums very well so I am using a .Java file
enum gameState {
  START,
  END,
  FALLING_OBJECTS //the first level
};